<form method="post" action="{{ path('app_admin_article_delete', {'id': article.id}) }}" class="admin-delete-form" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet article ? Cette action est irréversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <div class="admin-delete-warning">
        <i class="fas fa-exclamation-triangle"></i>
        <p>Attention : La suppression d'un article est définitive.</p>
    </div>
    <button type="submit" class="admin-card-button danger">
        <i class="fas fa-trash"></i> Supprimer
    </button>
</form>
